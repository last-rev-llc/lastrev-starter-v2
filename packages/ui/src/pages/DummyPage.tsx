import React from 'react';

// Cypress next framework needs to see a pages directory for some reason,
// so we're just going to use this as a dummy page.

const DummyPage = () => {
  return <div>Dummy Page</div>;
};

export default DummyPage;
